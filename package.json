{
	"name": "chatbot",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"postinstall": "node -e \"const fs = require('fs'); if (!fs.existsSync('profile.json')) fs.copyFileSync('profile.example.json', 'profile.json');\"",
		"setup-vectors": "wrangler deploy scripts/setup-profile-vectors.js"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"vitest": "~3.2.0",
		"wrangler": "^4.59.1"
	},
	"dependencies": {
		"hono": "^4.11.4"
	}
}
